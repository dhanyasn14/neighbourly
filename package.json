{
  "name": "neighborly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm start --prefix server\" \"npm start --prefix client\"",
    "start": "concurrently -n SERVER,CLIENT -c green,cyan \"npm start --prefix server\" \"npm start --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "server": "npm start --prefix server",
    "server:watch": "npm run dev --prefix server",
    "client": "npm start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
